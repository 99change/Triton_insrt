# note of workflow

写一个伪装程序，截取ptx，调用插装脚本，会把整个ptx解析，知道需要在哪个地方插装

然后插入三种桩子（初始化，开始，结束），在python层的参数列表里申明一个buffer（？问题所在）,然后这个在ptx层会多一个指针，用来我们来写需要记录的数据。

使用pytorch的tensor，然后就可以取出来，然后进行各种操作。

在基本块上操作，更细的是用bra的内容要单独插装

还有一个探针：专门读取获取自动的参数

# 对比proton
修改输出的raw的格式

_dummy的部分修改为16和64位

chrome trace 格式，使用perfetto查看

添加一个配置文件用来控制具体的py设置

单独加一个loc到probe 编号的映射

加入无用桩子的消除，合并